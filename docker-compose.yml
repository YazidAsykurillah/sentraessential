services:

  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    container_name: sentraessential-app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./:/var/www
    networks:
      - sentraessential-network

  nginx:
    image: nginx:alpine
    container_name: sentraessential-nginx
    restart: unless-stopped
    depends_on:
      - app
    volumes:
      - ./:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - sentraessential-network

  mysql:
    image: mysql:8.4
    container_name: sentraessential-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: sentraessential
      MYSQL_USER: laravel
      MYSQL_PASSWORD: laravel
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - sentraessential-network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: sentraessential-phpmyadmin
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      PMA_HOST: mysql
    networks:
      - sentraessential-network

  mailpit:
    image: axllent/mailpit
    container_name: sentraessential-mailpit
    restart: unless-stopped
    ports:
      - "1025:1025"
      - "8025:8025"
    networks:
      - sentraessential-network

  node:
    image: node:22-alpine
    container_name: sentraessential-node
    user: "${UID:-1000}:${GID:-1000}"
    working_dir: /var/www
    tty: true
    ports:
      - "5173:5173"
    volumes:
      - ./:/var/www
    networks:
      - sentraessential-network

volumes:
  mysql_data:

networks:
  sentraessential-network:
    driver: bridge